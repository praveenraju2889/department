server:
  port: '8081'
  
spring:
  profiles:
    active: awsrds
---

spring:
  config:
    activate:
      on-profile: dev
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    url: jdbc:mysql://localhost:3306/dcbapp
    password: Sackatha@10
  main:
    banner-mode: log
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: 'true'
welcome:
  message: '"dev version 1.1"'
  
---  
spring:
  config:
    activate:
      on-profile: qa
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    url: jdbc:mysql://localhost:3306/dcbapp-qa
    password: Sackatha@10
  main:
    banner-mode: log
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: 'true'
welcome:
  message: '"qa version 1.1"' 
management:
  endpoints:
    web:
      exposure:
        include: "*"
        exclude: "env,beans" 
  
---   
spring:
  config:
    activate:
      on-profile: aws
  h2:
    console:
      enabled: 'true'
  datasource:
    username: sa
    url: jdbc:h2:mem:dcbapp
    driverClassName: org.h2.Driver
    password: password
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
welcome:
  message: '"aws version 1.1"'  
  
---   
spring:
  config:
    activate:
      on-profile: awsrds
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    username: admin
    url: jdbc:mysql://database-openshift.cb620aa6cm88.me-central-1.rds.amazonaws.com:3306/openshiftdb
    password: password
  main:
    banner-mode: log
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: 'true'
welcome:
  message: '"awsrds version 1.2"'

